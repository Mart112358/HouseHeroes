mutation CreateTask($input: CreateTaskInput!) {
  createTask(input: $input) {
    id
    title
    description
    dueDate
    isCompleted
    createdAt
    family {
      id
      name
    }
    createdBy {
      id
      firstName
      lastName
    }
  }
}