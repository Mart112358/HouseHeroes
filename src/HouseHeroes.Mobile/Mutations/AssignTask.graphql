mutation AssignTask($input: AssignTaskInput!) {
  assignTask(input: $input) {
    taskId
    userId
    task {
      id
      title
    }
    user {
      id
      firstName
      lastName
    }
  }
}